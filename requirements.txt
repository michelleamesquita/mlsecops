pandas>=2.2.0
scikit-learn>=1.5.0
joblib>=1.4.0
mlflow>=2.13.0
numpy>=1.26.0
pyarrow>=15.0.0
pyyaml>=6.0.0

# ── Data Quality (Estágio 01) ─────────────────────────────────────────────────
great-expectations>=0.18.0
cleanlab>=2.6.0
evidently>=0.4.30
scipy>=1.13.0             # KS test, Chi-squared (poison_detection.py)

# ── Supply-chain audit (DevSecOps) ───────────────────────────────────────────
pip-audit>=2.7.0
nbdefense>=0.0.7          # Jupyter notebook secrets/PII/CVE scanner (NB Defense)

# ── Model scanning (Estágio 02) ───────────────────────────────────────────────
modelscan>=0.8.0          # ProtectAI — backdoor/malicious code detection

# ── Adversarial evaluation — IBM ART (Estágio 03) ────────────────────────────
adversarial-robustness-toolbox>=1.18.0

# ── LLM security (quando aplicável) ──────────────────────────────────────────
# garak>=0.9.0            # NVIDIA — LLM vulnerability scanner (instalar separado)

# ── SBOM ─────────────────────────────────────────────────────────────────────
cyclonedx-bom>=4.0.0

# ── Provenance / DVC (opcional) ──────────────────────────────────────────────
# dvc>=3.0.0
